---
'@backstage/plugin-scaffolder-backend-module-github': patch
---

Add optional `autoInit` input to `github:repo:create` and `publish:github` actions to enable GitHub's `auto_init` (initial README) when creating repositories.
